/**
 * Initializes relative time updates for individual post pages
 */

import { initRelativeTimes } from './utils/relativeTimeUpdater.js';

// Initialize relative times on page load
initRelativeTimes();

